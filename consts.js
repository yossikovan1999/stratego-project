const CHARACTER_NUMBERS = [...Array(10).keys()];

const MAX_ROWS = 10;

const MAX_COLS = 10;

const EMPTY_CELL = "_";

const DIRECTION = {up : [-1, 0], down : [1, 0], right : [0,1], left :[0, -1]};

export { CHARACTER_NUMBERS, MAX_COLS, MAX_ROWS, EMPTY_CELL, DIRECTION };
